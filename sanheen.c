#include<stdio.h>
int main(){
    char not;
    not = !45;
    printf("%u",not);
}
// it signifies that any character of 'char' type could have char and int possible type only, %f will give error

outut : 0 for %d,%u
        nothing for %c
